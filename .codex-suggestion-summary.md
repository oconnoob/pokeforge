Dialed back fire move damage by introducing a type-specific tuning in the battle engine.
- Added `TYPE_POWER_ADJUSTMENTS` so fire-type moves receive a 0.92 multiplier before other modifiers are applied, curbing their overall damage without touching other types (packages/battle-engine/src/index.ts:271, packages/battle-engine/src/index.ts:352).
- Extended the combat engine unit suite to lock in the new fire tuning and keep the calculation deterministic (packages/battle-engine/tests/engine.test.ts:74).

Assumption: the report targeted all fire-type move damage, so a uniform fire-only multiplier is the least invasive fix.

Tests:
- `npm run test --workspace @pokeforge/battle-engine`
- `npm run test:unit`

Next steps:
1. Monitor match telemetry or user feedback to ensure fire win rates fall into the expected range without underpowering the type.
