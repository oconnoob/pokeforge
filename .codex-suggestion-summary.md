Dialed back fire-type damage output to reflect the report.
- `packages/battle-engine/src/index.ts:271` adds a `TYPE_DAMAGE_MODIFIER` map and applies a 0.96 multiplier inside `calculateBaseDamage` so fire moves land slightly softer while keeping other types unchanged.
- `packages/battle-engine/tests/engine.test.ts:74` introduces a regression test that compares neutral fire and water hits, preventing the higher fire damage from returning.

Assumption: Interpreted “fire Pokémon do too much damage” as a request for a small global fire-type damage nerf, so I applied a modest 4% reduction rather than reworking move stats or the type chart.

Tests: `npm run test --workspace @pokeforge/battle-engine`.
